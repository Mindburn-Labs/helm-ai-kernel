# Shipped binaries: the roots of the deadcode gate (scripts/ci/deadcode_gate.sh).
# Target architecture R10: reachability runs from the declared shipped entry
# points. A main belongs here when a release artifact is built from it.
#
# core/cmd/helm-ai-kernel: release.yml binaries and the container image.
#
# Not roots, because no release path builds them: receipt_verify (only the
# stand-alone `make receipt-verify-binaries`), release-permit-verify,
# tee-collateral, pack_verify, channel_gateway, bootstrap, semantic-model-gen,
# skill_lint, skill_pack. Code only they reach counts as dead to the gate. Add a
# line here when one of them starts shipping.
core/cmd/helm-ai-kernel
