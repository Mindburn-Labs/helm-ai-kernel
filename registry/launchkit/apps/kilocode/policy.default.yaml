network:
  default: deny
  allow: []
filesystem:
  default: deny
  allow:
    - workspace:rw
    - app_state:rw
mcp:
  unknown_server_policy: quarantine
  unknown_tool_policy: ESCALATE
  require_schema_pin: true
