name: Maven Publish

on:
  workflow_dispatch:
    inputs:
      version:
        description: Version to publish. Must match VERSION.
        required: true
        type: string

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true


jobs:
  publish:
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    environment: maven-central
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 # v5
        with:
          distribution: temurin
          java-version: "21"
      - name: Validate dispatch inputs
        run: |
          python3 scripts/ci/validate_workflow_dispatch_inputs.py publish \
            --version "${{ inputs.version }}"
      - name: Version checks
        working-directory: sdk/java
        run: |
          FILE_VERSION=$(cat ../../VERSION)
          REQUESTED="${{ github.event.inputs.version }}"
          POM_VERSION=$(grep -m1 '<version>' pom.xml | sed 's/.*<version>\([^<]*\)<\/version>.*/\1/')
          test "$REQUESTED" = "$FILE_VERSION"
          test "$POM_VERSION" = "$FILE_VERSION"
      - name: Check if version already published
        id: precheck
        working-directory: sdk/java
        run: |
          set -euo pipefail
          VER=$(cat ../../VERSION)
          URL="https://repo1.maven.org/maven2/io/github/mindburnlabs/helm-sdk/${VER}/helm-sdk-${VER}.pom"
          code=$(curl -sS -o /dev/null -w "%{http_code}" "$URL")
          if [ "$code" = "200" ]; then
            echo "helm-sdk ${VER} is already in Maven Central — skipping publish."
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            echo "helm-sdk ${VER} not in Central yet (HTTP ${code}) — proceeding with publish."
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Build
        if: steps.precheck.outputs.skip != 'true'
        working-directory: sdk/java
        run: mvn -q test package
      - name: Import GPG key
        if: steps.precheck.outputs.skip != 'true'
        env:
          MAVEN_GPG_PRIVATE_KEY: ${{ secrets.MAVEN_GPG_PRIVATE_KEY }}
        run: |
          set -euo pipefail
          key_file="$RUNNER_TEMP/maven-gpg-private-key"
          printf '%s' "$MAVEN_GPG_PRIVATE_KEY" > "$key_file"
          if gpg --batch --import "$key_file"; then
            exit 0
          fi
          if base64 --decode "$key_file" > "${key_file}.decoded" 2>/dev/null && gpg --batch --import "${key_file}.decoded"; then
            exit 0
          fi
          printf '%b' "$MAVEN_GPG_PRIVATE_KEY" > "${key_file}.escaped"
          gpg --batch --import "${key_file}.escaped"
      - name: Publish GPG public key
        if: steps.precheck.outputs.skip != 'true'
        run: |
          set -euo pipefail
          fingerprint="$(gpg --batch --list-secret-keys --with-colons | awk -F: '$1 == "fpr" { print $10; exit }')"
          test -n "$fingerprint"
          echo "MAVEN_GPG_KEY_FINGERPRINT=$fingerprint" >> "$GITHUB_ENV"
          gpg --batch --armor --export "$fingerprint" > "$RUNNER_TEMP/maven-public-key.asc"

          sent=0
          for keyserver in hkps://keyserver.ubuntu.com hkps://keys.openpgp.org; do
            if timeout 60s gpg --batch --keyserver "$keyserver" --send-keys "$fingerprint"; then
              sent=1
            else
              echo "::warning::Could not upload Maven public key to $keyserver"
            fi
          done
          test "$sent" = "1"

          verify_home="$RUNNER_TEMP/gpg-keyserver-verify"
          mkdir -p "$verify_home"
          chmod 700 "$verify_home"
          for attempt in 1 2 3; do
            if GNUPGHOME="$verify_home" timeout 60s gpg --batch --keyserver hkps://keyserver.ubuntu.com --recv-keys "$fingerprint"; then
              exit 0
            fi
            echo "::warning::Maven public key verification attempt $attempt failed"
            sleep 10
          done
          exit 1
      - name: Publish
        if: steps.precheck.outputs.skip != 'true'
        working-directory: sdk/java
        env:
          MAVEN_USERNAME: ${{ secrets.MAVEN_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}
          OSSRH_USERNAME: ${{ secrets.MAVEN_USERNAME }}
          OSSRH_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}
          MAVEN_GPG_PRIVATE_KEY: ${{ secrets.MAVEN_GPG_PRIVATE_KEY }}
          MAVEN_GPG_PASSPHRASE: ${{ secrets.MAVEN_GPG_PASSPHRASE }}
        run: >
          mvn --batch-mode deploy
          --settings ../../scripts/release/maven-settings.xml
          -Dgpg.keyname="$MAVEN_GPG_KEY_FINGERPRINT"
          -Dgpg.passphrase="$MAVEN_GPG_PASSPHRASE"
