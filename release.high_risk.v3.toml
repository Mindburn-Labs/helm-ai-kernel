name = "release.high_risk.v3"
profile = "high_risk"
# Compliance mapping only: this pack defines no runtime actions, so execution
# remains fail-closed until an explicit supported runtime policy is supplied.
reference_pack = "./reference_packs/eu_ai_act_high_risk.v2.json"

[server]
bind = "127.0.0.1"
port = 7714

[receipts]
store = "sqlite"
path = "./data/receipts.db"
