{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://helm.mindburn.run/schemas/effects/launch/provider_provision.v1.json",
  "version": "1.1.0-alpha.1",
  "title": "Provider Provision Effect Input (Preview)",
  "description": "Provider-neutral preview input for creating the exact resource graph selected by an approved RouteBinding.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version", "effect_id", "tenant_id", "workspace_id", "mission_id", "effect_ordinal",
    "provider", "provider_account_ref", "provider_account_hash", "region", "jurisdiction",
    "repository_analysis_ref", "repository_analysis_hash", "workload_graph_ref", "workload_graph_hash",
    "route_binding_ref", "route_binding_hash", "route_placement_id", "provider_capability_profile_ref", "provider_capability_profile_hash",
    "provider_certification_ref", "provider_certification_hash",
    "provider_connector_id", "provider_connector_contract_hash", "provider_action_urn", "provider_payload_hash",
    "resource_graph_hash", "billing_cadence", "commitment_term", "gross_cap_currency", "gross_cap_minor",
    "gross_exposure_minor", "generated_spec_hash", "route_quote_ref", "route_quote_hash", "constraint_set_hash",
    "spend_intent_hash", "spend_envelope_hash", "budget_verdict_receipt_hash", "budget_reservation_ref",
    "budget_reservation_hash", "provider_terms_profile_hash", "compensation_graph_hash",
    "failure_teardown_policy_hash", "teardown_authority_mode", "plan_hash", "connector_contract_hash"
  ],
  "properties": {
    "schema_version": { "const": "launch_effect_input.v1" },
    "effect_id": { "const": "PROVIDER_PROVISION" },
    "tenant_id": { "$ref": "#/$defs/non_empty" },
    "workspace_id": { "$ref": "#/$defs/non_empty" },
    "mission_id": { "$ref": "#/$defs/non_empty" },
    "effect_ordinal": { "type": "integer", "minimum": 0 },
    "provider": { "$ref": "#/$defs/non_empty" },
    "provider_account_ref": { "$ref": "#/$defs/non_empty" },
    "provider_account_hash": { "$ref": "#/$defs/hash" },
    "region": { "$ref": "#/$defs/non_empty" },
    "jurisdiction": { "$ref": "#/$defs/non_empty" },
    "repository_analysis_ref": { "$ref": "#/$defs/non_empty" },
    "repository_analysis_hash": { "$ref": "#/$defs/hash" },
    "workload_graph_ref": { "$ref": "#/$defs/non_empty" },
    "workload_graph_hash": { "$ref": "#/$defs/hash" },
    "route_binding_ref": { "$ref": "#/$defs/non_empty" },
    "route_binding_hash": { "$ref": "#/$defs/hash" },
    "route_placement_id": { "$ref": "#/$defs/non_empty" },
    "provider_capability_profile_ref": { "$ref": "#/$defs/non_empty" },
    "provider_capability_profile_hash": { "$ref": "#/$defs/hash" },
    "provider_certification_ref": { "$ref": "#/$defs/non_empty" },
    "provider_certification_hash": { "$ref": "#/$defs/hash" },
    "provider_connector_id": { "$ref": "#/$defs/non_empty" },
    "provider_connector_contract_hash": { "$ref": "#/$defs/hash" },
    "provider_action_urn": { "$ref": "#/$defs/non_empty" },
    "provider_payload_hash": { "$ref": "#/$defs/hash" },
    "resource_graph_hash": { "$ref": "#/$defs/hash" },
    "billing_cadence": { "$ref": "#/$defs/billing_cadence" },
    "commitment_term": { "$ref": "#/$defs/commitment_term" },
    "gross_cap_currency": { "$ref": "#/$defs/currency" },
    "gross_cap_minor": { "type": "integer", "minimum": 0 },
    "gross_exposure_minor": { "type": "integer", "minimum": 0 },
    "generated_spec_hash": { "$ref": "#/$defs/hash" },
    "route_quote_ref": { "$ref": "#/$defs/non_empty" },
    "route_quote_hash": { "$ref": "#/$defs/hash" },
    "constraint_set_hash": { "$ref": "#/$defs/hash" },
    "spend_intent_hash": { "$ref": "#/$defs/hash" },
    "spend_envelope_hash": { "$ref": "#/$defs/hash" },
    "budget_verdict_receipt_hash": { "$ref": "#/$defs/hash" },
    "budget_reservation_ref": { "$ref": "#/$defs/non_empty" },
    "budget_reservation_hash": { "$ref": "#/$defs/hash" },
    "provider_terms_profile_hash": { "$ref": "#/$defs/hash" },
    "compensation_graph_hash": { "$ref": "#/$defs/hash" },
    "failure_teardown_policy_hash": { "$ref": "#/$defs/hash" },
    "teardown_authority_mode": { "const": "FRESH_DUAL_CONTROL_REQUIRED" },
    "plan_hash": { "$ref": "#/$defs/hash" },
    "connector_contract_hash": { "$ref": "#/$defs/hash" }
  },
  "$defs": {
    "hash": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
    "non_empty": { "type": "string", "minLength": 1 },
    "currency": { "type": "string", "pattern": "^[A-Z]{3}$" },
    "billing_cadence": { "enum": ["HOURLY", "DAILY", "MONTHLY", "USAGE_BASED", "ONE_TIME"] },
    "commitment_term": { "enum": ["NONE", "MONTH_TO_MONTH", "ANNUAL", "MULTI_YEAR"] }
  },
  "x-helm": {
    "status": "preview",
    "execution_enabled": false,
    "category": "launch",
    "risk_class": "E3",
    "approval_level": "single_human",
    "reversible": true,
    "reconciliation_required": true,
    "provider_neutral": true,
    "provider_profile_validation_required": true,
    "base_effect_types": ["INFRA_CHANGE", "BILLING_CHANGE", "EXTERNAL_API_CALL"]
  }
}
