# Compatibility Package

`packages/design-system-core` is a temporary compatibility package. It is not
the canonical Mindburn design-system source.

New source tokens and generated package outputs start in
`platform-design-system` as `@mindburn/design`. Existing consumers can keep this
package until their imports move to `@mindburn/design/*`.
