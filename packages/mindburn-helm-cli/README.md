# @mindburn/helm-cli

JavaScript CLI for local evidence-bundle verification and conformance-oriented reporting.

## Install

```bash
npm install @mindburn/helm-cli
```

## Local Development

```bash
npm ci
npm test -- --run
npm run build
```

The package source lives in `src/` and is used by the fixture verification path in the repository.
