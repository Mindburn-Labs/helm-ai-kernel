{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://helm.mindburn.run/schemas/access/PrivilegedAccessReceipt.v1.json",
  "title": "PrivilegedAccessReceipt",
  "description": "Immutable receipt for privileged access grant with audit trail",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "receipt_id", "request_id", "grant", "grantee", "attestation"],
  "properties": {
    "version": {
      "type": "string",
      "const": "1.0.0"
    },
    "receipt_id": {
      "type": "string",
      "pattern": "^prec-[a-z0-9-]+$"
    },
    "request_id": {
      "type": "string",
      "description": "Reference to the originating PrivilegedAccessRequest"
    },
    "grant": {
      "type": "object",
      "additionalProperties": false,
      "required": ["granted_tier", "granted_at", "expires_at", "granted_by"],
      "properties": {
        "granted_tier": {"type": "string"},
        "granted_at": {"type": "string", "format": "date-time"},
        "expires_at": {"type": "string", "format": "date-time"},
        "granted_by": {
          "type": "array",
          "minItems": 1,
          "items": {"type": "string"},
          "description": "Identity IDs of approvers"
        },
        "granted_scope": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "packs": {"type": "array", "items": {"type": "string"}},
            "environments": {"type": "array", "items": {"type": "string"}},
            "actions": {"type": "array", "items": {"type": "string"}}
          }
        }
      }
    },
    "grantee": {
      "type": "object",
      "additionalProperties": false,
      "required": ["identity_id"],
      "properties": {
        "identity_id": {"type": "string"},
        "email": {"type": "string", "format": "email"},
        "name": {"type": "string"}
      }
    },
    "session": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "session_id": {"type": "string"},
        "started_at": {"type": "string", "format": "date-time"},
        "ended_at": {"type": "string", "format": "date-time"},
        "termination_reason": {
          "type": "string",
          "enum": ["expired", "revoked", "completed", "error"]
        }
      }
    },
    "actions_taken": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "action": {"type": "string"},
          "target": {"type": "string"},
          "timestamp": {"type": "string", "format": "date-time"},
          "result": {"type": "string", "enum": ["success", "failure", "denied"]},
          "details": {
            "type": "object",
            "additionalProperties": {
              "type": ["string", "number", "integer", "boolean", "null"]
            }
          }
        }
      },
      "description": "Log of actions performed during privileged session"
    },
    "revocation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "revoked": {"type": "boolean"},
        "revoked_at": {"type": "string", "format": "date-time"},
        "revoked_by": {"type": "string"},
        "reason": {"type": "string"}
      }
    },
    "attestation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["hash", "algorithm", "signature", "signed_by"],
      "properties": {
        "hash": {
          "type": "string",
          "pattern": "^(sha256:)?[a-f0-9]{64}$",
          "description": "SHA256 hash of receipt content"
        },
        "algorithm": {
          "type": "string",
          "const": "sha256"
        },
        "signature": {
          "type": "string",
          "pattern": "^(ed25519|ecdsa-p256|rsa-pss):[A-Za-z0-9+/_=-]+$",
          "description": "Cryptographic signature"
        },
        "signed_by": {
          "type": "string",
          "pattern": "^(did:[A-Za-z0-9:._%#-]+|key:[A-Za-z0-9._:-]+)$",
          "description": "Key ID used for signing"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time"
        }
      }
    }
  }
}
